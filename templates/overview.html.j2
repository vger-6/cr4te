<!DOCTYPE html>
<html lang="en">
<head>
  <style>body { display: none; }</style>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ html_settings.overview_page_title }}</title>
  <link rel="stylesheet" href="css/base.css">
  <link rel="stylesheet" href="css/creator.css">
</head>
<body>

<div class="top-link">
  <span class="nav-current">{{ html_settings.nav_creators_label }}</span> &middot;
  <a href="projects.html">{{ html_settings.nav_projects_label }}</a> &middot;
  <a href="tags.html">{{ html_settings.nav_tags_label }}</a>
  <div style="float: right;">
    <div class="theme-dropdown">
      <button class="theme-button" id="theme-toggle">Themes ▾</button>
      <div class="theme-panel" id="theme-panel">
        <div class="theme-option" data-theme="theme-frozen-aurora">Frozen Aurora</div>
        <div class="theme-option" data-theme="theme-forest-night">Forest Night</div>
        <div class="theme-option" data-theme="theme-midnight-monochrome">Midnight Mono</div>
        <div class="theme-option" data-theme="theme-warm-linen">Warm Linen</div>
        <div class="theme-option" data-theme="theme-sunset-neon">Sunset Neon</div>
      </div>
    </div>
  </div>
</div>

<h1>{{ html_settings.overview_page_title }}</h1>

<div class="search-bar-wrapper">
  <input type="text" id="search-input" placeholder="{{ html_settings.overview_page_search_placeholder }}" class="search-box">
</div>

<div class="creator-box">
  <hr>
  <div class="creator-gallery">

    {% for creator in creator_entries %}
    <div class="creator-entry" data-search="{{ creator.search_text }}">
      <a href="{{ creator.url }}" title="{{ creator.name }}">
        <img src="{{ creator.thumbnail_url }}" width="100" alt="{{ creator.name }} thumbnail"><br>
        {{ creator.name }}
      </a>
    </div>
    {% endfor %}

  </div>
</div>

<script src="js/filter.js"></script>
<script src="js/theme.js" defer></script>

</body>
</html>

