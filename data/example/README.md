# Example Music Library

This directory contains a small music-domain cr4te library used for manual testing and demos.

Build it with:

```bash
cr4te build -i data/example/Musicians -o output/example-site --domain music
```

The sample includes solo musicians, a collaboration, albums with audio tracks, cover art, portrait images, and Markdown descriptions.
